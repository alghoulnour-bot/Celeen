PORT=3000

# REQUIRED to enable the responses dashboard at /admin (disabled if unset).
# Generate a strong value: openssl rand -hex 32
ADMIN_KEY=

# Filled in later, once the couple's Stripe account is live (see src/routes/stripeWebhook.js)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
