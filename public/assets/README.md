Drop the AI-generated invite video here as `invite-video.mp4`.

Until it's added, the "Tap to Open" envelope still works — the site
detects the missing/failed video and skips straight to the main content.
